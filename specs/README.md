# Specs

This repo uses a spec-driven flow:

1. Product behavior is written down here first.
2. Architecture and acceptance criteria are derived from the spec.
3. Code changes in `android/`, `web/`, and `db/` should point back to the relevant spec.

Current primary spec:

- `001-frame-stream/spec.md`
